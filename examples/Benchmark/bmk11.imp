@Table(emp, n:str, r:str, s:int)@
@Policy(SELECT n,s FROM emp; | SELECT n,r FROM emp; )@

x = @Query(SELECT FROM emp WHERE n = 'Alice' AND r = 'CEO';)@;

if (x == True) then
{
    out(1,usr);
}
else
{
    out(1,usr);
}